/**
 * 共享 State 定义导出
 *
 * 多个 Agent 需要共享的 LangGraph State Annotation 在此定义。
 *
 * 示例：
 * export { BaseAgentAnnotation } from './base-agent.state';
 * export { ChatAnnotation } from './chat.state';
 */
