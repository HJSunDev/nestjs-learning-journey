/**
 * Agent 类型定义（预留）
 *
 * 智能体（Agent）的构建将完全基于 LangGraph 实现：
 * - 单个 Agent：通过 LangGraph 的 StateGraph 定义节点和边
 * - 多 Agent 协作：通过 LangGraph 的 Subgraph、Handoffs 等机制编排
 *
 * Agent 的核心类型（State、Node、Edge、Annotation）由 @langchain/langgraph 包提供，
 * 此文件预留给未来章节中可能需要的业务级辅助类型定义。
 *
 * @see 后续章节：LangGraph 智能体构建
 */
